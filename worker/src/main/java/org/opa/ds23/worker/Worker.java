package org.opa.ds23.worker;

/**
 * Main Worker Class (worker app entry point)
 */
public class Worker {
  public static void main(String[] args) {
    //Read parameters:
    //- number of threads
    //- worker TCP machine:port

    //initialize thread pool
    //connect to server and listen for commands
  }
}